import React from 'react';
import ReactLoading from 'react-loading';

const LoadingModule = () => (
	<ReactLoading type="cylon" color="var(--color-primary-dark)" height={247} width={55} />
);

export default LoadingModule;